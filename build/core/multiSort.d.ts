/**
 * sort array based on multiple criteria
 *
 * @export
 * @template T
 * @param {T[]} data
 * @param {string[]} comparator
 * @return {*}  {T[]}
 */
export declare function sort<T>(data: T[], comparator: string[]): T[];
