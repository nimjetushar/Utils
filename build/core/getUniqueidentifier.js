/**
 * returns random string which can be used as an unique identifier
 *
 * @export
 * @return {*}  {string}
 */
export function getUiqueIdentifier() {
    return Math.random().toString(16).slice(2);
}
//# sourceMappingURL=getUniqueidentifier.js.map